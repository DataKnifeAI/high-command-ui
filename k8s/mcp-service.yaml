apiVersion: v1
kind: Service
metadata:
  name: high-command-mcp
  namespace: high-command
  labels:
    app: high-command-mcp
spec:
  type: ExternalName
  externalName: high-command-mcp.mcp-servers.svc.cluster.local
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
