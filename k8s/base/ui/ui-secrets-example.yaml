apiVersion: v1
kind: Secret
metadata:
  name: high-command-ui-secrets
  namespace: high-command
type: Opaque
stringData:
  # vite-claude-api-key: Optional, for Claude. Must be set at BUILD time (Vite), not runtime.
